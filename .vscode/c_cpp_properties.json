{
    "configurations": [
        {
            "name": "Win32",// this is the name of the configuration
            "includePath": [
                "${workspaceFolder}/include",
                "${workspaceFolder}/src",
                "${workspaceFolder}/external/raylib/src"
            ],// thisis the include path for the project
            "cppStandard": "c++17",// this is the C++ standard to use
            "intelliSenseMode": "windows-msvc-x64"// this is the IntelliSense mode to use
        }
    ],
    "version": 4// this is the version of the configuration file
}

// This file is used by VS Code to configure the C/C++ extension.
